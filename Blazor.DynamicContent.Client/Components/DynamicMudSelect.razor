@typeparam T

<MudSelect Label="@Label" Value="Value" ValueChanged="(T value) => OnSelectionChanged(value)">
    @foreach(var item in Items)
    {
        <MudSelectItem Value="item.Key">@item.Value</MudSelectItem>
    }
</MudSelect>